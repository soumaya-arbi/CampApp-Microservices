spring.application.name=reclamationService

# ------------------------
# PostgreSQL Configuration (Production) - ACTIF
# ------------------------
spring.datasource.url=jdbc:postgresql://localhost:5432/reclamationdb
spring.datasource.username=postgres
spring.datasource.password=camp123
spring.datasource.driver-class-name=org.postgresql.Driver

# ------------------------
# H2 Configuration (Development/Testing) - COMMENTÉ
# ------------------------
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.driver-class-name=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# ------------------------
# JPA / Hibernate
# ------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ------------------------
# Server
# ------------------------
server.port=8085

# ------------------------
# Eureka Client Configuration
# ------------------------
eureka.client.service-url.defaultZone=http://localhost:8761/eureka

# ------------------------
# Logging (optionnel)
# ------------------------
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# ------------------------
# Spring Cloud Config
# ------------------------
spring.cloud.config.enabled=false
spring.config.import-check.enabled=false
spring.config.import=optional:configserver:
